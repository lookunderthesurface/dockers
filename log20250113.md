# clash
祖传clash

# Docker
安装docker toolkit https://docs.nvidia.com/datacenter/cloud-native/container-toolkit/latest/install-guide.html
安装docker https://docs.docker.com/engine/install/ubuntu/#uninstall-docker-engine
配置docker代理 https://blog.csdn.net/qq_45975878/article/details/143161306
拉取镜像 https://hub.docker.com/r/pytorch/pytorch/tags docker pull pytorch/pytorch:2.5.1-cuda12.4-cudnn9-devel
创建实例 docker run --gpus all -it --network host --shm-size=8g --name torch pytorch/pytorch:2.5.1-cuda12.4-cudnn9-devel bash
实例中配置代理 export http_proxy=http://127.0.0.1:7890 && export https_proxy=http://127.0.0.1:7890
配置git git config --global user.name "HuWencan" && git config --global user.email "l8lssq3l7o@sjtu.edu.cn"

# SSH
配置ssh https://blog.csdn.net/qq_42568323/article/details/141106649
PermitRootLogin no
AllowUsers huwencan
MaxAuthTries 3

# github
git config --global user.name "lkudsurf" && git config --global user.email "l8lssq3l7o@sjtu.edu.cn"

# 路由器局域网分配passthrough ipv6地址

# ddns go 127.0.0.1:9876
```bash
docker run -d \
  --name ddns-go \
  --restart unless-stopped \
  --net=host \
  -v /opt/ddns-go:/root \
  jeessy/ddns-go
```

# docker compose?
加入失败重启
version: "3.8"
services:
  ddns-go:
    image: jeessy/ddns-go
    container_name: ddns-go
    network_mode: host
    restart: on-failure
    volumes:
      - /opt/ddns-go:/root
    healthcheck:
      test: ["CMD-SHELL", "grep -q '未能获取IPv6地址' /root/logs/ddns-go.log && exit 1 || exit 0"]
      interval: 30s
      timeout: 3s
      retries: 3

# npm
## 启动
```bash
docker run -d \
  --name nginx_proxy \
  --network host \
  -v /home/huwencan/workspace/npm_data:/data \
  -v /home/huwencan/workspace/letsencrypt:/etc/letsencrypt \
  --restart unless-stopped \
  jc21/nginx-proxy-manager:latest
```
## 账号更改
admin@example.com
changeme
1815593176@qq.com
hu2578925789
## 配置npm 127.0.0.1:81
http 127.0.0.1 8080 block common exploits
force ssl http2 hsts
手动上传腾讯ssl证书,到期需在服务器设置新证书
腾讯托管证书 https://console.cloud.tencent.com/ssl/dsc/hosting

# Caddy (代替npm)
```bash
mkdir -p /home/huwencan/workspace/caddy-docker/{data,config}
nano /home/huwencan/workspace/caddy-docker/Caddyfile
sudo chmod -R 777 /home/huwencan/workspace/caddy-docker/
```

## Caddyfile
```nginx
{
    email 1815593176@qq.com
    acme_ca https://acme-v02.api.letsencrypt.org/directory
}

nextcloud.lkudsurf.online {
    redir https://{host}{uri} permanent
    reverse_proxy 127.0.0.1:8080
}

sunshine.lkudsurf.online:47984 {
    redir https://{host}{uri} permanent
    reverse_proxy 192.168.5.251:47984
}
sunshine.lkudsurf.online:47989 {
    redir https://{host}{uri} permanent
    reverse_proxy 192.168.5.251:47989
}
sunshine.lkudsurf.online:47990 {
    redir https://{host}{uri} permanent
    reverse_proxy 192.168.5.251:47990
}
sunshine.lkudsurf.online:48010 {
    redir https://{host}{uri} permanent
    reverse_proxy 192.168.5.251:48010
}
sunshine.lkudsurf.online:47998 {
    redir https://{host}{uri} permanent
    reverse_proxy 192.168.5.251:47998 
}
sunshine.lkudsurf.online:47999 {
    redir https://{host}{uri} permanent
    reverse_proxy 192.168.5.251:47999
}
sunshine.lkudsurf.online:48000 {
    redir https://{host}{uri} permanent
    reverse_proxy 192.168.5.251:48000 
}
```
路由器局域网配置静态地址绑定
## docker run
```bash
docker run -d \
  --name caddy \
  --restart unless-stopped \
  --network host \
  -v /home/huwencan/workspace/caddy-docker/data:/data \
  -v /home/huwencan/workspace/caddy-docker/config:/config \
  -v /home/huwencan/workspace/caddy-docker/Caddyfile:/etc/caddy/Caddyfile \
  caddy:latest
```

# nextcloud 127.0.0.1:8080
## 预设Apache配置
```bash
mkdir -p nano /home/huwencan/workspace/nextcloud/config
nano /home/huwencan/workspace/nextcloud/config/ports.conf
```

```bash
Listen 8080
<IfModule ssl_module>
    Listen 8443
</IfModule>
```
## 启动，occ配置Nextcloud应用
```bash
docker run -d \
  --name nextcloud \
  --restart unless-stopped \
  --network host \
  -v /home/huwencan/workspace/nextcloud/data:/var/www/html \
  -v /home/huwencan/workspace/nextcloud/config/ports.conf:/etc/apache2/ports.conf:ro \
  nextcloud:latest
```

```bash
docker run -d \
  --name nextcloud-aio-mastercontainer \
  --restart unless-stopped \
  --network host \
  -v nextcloud_aio_mastercontainer:/mnt/docker-aio-config \
  -v /var/run/docker.sock:/var/run/docker.sock:ro \
  nextcloud/all-in-one:latest
```

## php有初始化内容必须在容器启动后再补充
```bash
docker exec nextcloud su -s /bin/sh www-data -c "php occ config:system:set trusted_domains 1 --value='nextcloud.lkudsurf.online'"
```